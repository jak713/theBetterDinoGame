[project]
name = "group3-group-project"
version = "0.1.0"
description = "Chrome offline dino game clone"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pygame>=2.6.1",
    "pytest>=9.1.0",
]
[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
